import React from 'react';
import ReactDOM from 'react-dom';
import DispatchingApp from './containers/DispatchingApp';

ReactDOM.render(<DispatchingApp />, document.getElementById('dispatching-app'));